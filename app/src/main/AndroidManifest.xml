<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidstudynotes">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.WRITE_SMS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="MARKET_CODE"
            android:value="00000001" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.UIActivity" />
        <activity android:name=".ui.GifUIActivtiy" />
        <activity android:name=".ui.UserHeadActivity" />
        <activity android:name=".version.VersionMgrActivity" />
        <activity android:name=".memory.handler.HandlerMainActivity" />
        <activity android:name=".memory.handler.Handler2MainLoopActivity" />
        <activity android:name=".memory.handler.HandlerBaseActivity" />
        <activity android:name=".memory.handler.HandlerStaticActivity" />
        <activity android:name=".memory.handler.HandlerActivity" />
        <activity android:name=".memory.handler.HandlerWeakActivity" />
        <activity android:name=".memory.handler.HandlerWeakHandlerActivity" />
        <activity android:name=".debug.point.PointActivity" />
        <activity android:name=".codedesign.moduledesign.BaseModuleDesignActivity" />
        <activity android:name=".ui.PopUpMessageActivity" />
        <activity android:name=".ui.activity.ParticleActivity" />
        <activity android:name=".ui.activity.VIewListActivity" />
        <activity android:name=".ui.activity.FragmentViewPagerActivity" />
        <activity android:name=".sms.SMSWriterActivity"/>

        <receiver android:name=".sms.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver that listens for incoming MMS messages -->
        <receiver android:name=".sms.MmsReceiver"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>
        <activity android:name=".sms.ComposeSmsActivity" >
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>

        <!-- Service that delivers messages from the phone "quick response" -->
        <service android:name=".sms.HeadlessSmsSendService"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>
    </application>

</manifest>